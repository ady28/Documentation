version: "3.8"

services:
  mongodb:
    image: mongo:5.0.5
    volumes: 
      - mongodata:/data/db
  adiweb:
    build: .
    ports: 
      - '8080:8080'
    depends_on: 
      - mongodb

volumes:
  mongodata: