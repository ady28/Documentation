apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: goapimongo-pvc
  namespace: goapi
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 2Gi