apiVersion: v1
kind: Service
metadata:
  name: goapiapp-service
  namespace: goapi
spec:
  selector: 
    app: goapiapp
  type: ClusterIP
  ports:
    - protocol: "TCP"
      port: 8080
      targetPort: 8080