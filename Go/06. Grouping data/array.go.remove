package main

import "fmt"

func main() {

	//Arrays are not really used in go; instead slices should be used

	//Declare an array
	var x [4]int
	fmt.Println(x)
	x[3] = 42
	fmt.Println(x)
	fmt.Println(len(x))
}
