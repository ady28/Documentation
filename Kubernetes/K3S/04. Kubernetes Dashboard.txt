#Download: https://github.com/kubernetes/dashboard/blob/master/aio/deploy/recommended.yaml
kubectl apply -f dash.yaml
kubectl apply -f dash-admin-user.yaml
kubectl apply -f dash-admin-user-role.yaml

#Use this command to obtain the token for connecting to dashboard
kubectl -n kubernetes-dashboard describe secret admin-user-token | grep '^token'

#Now either modify the service to use NodePort or get the dashboard traffic out through an ingress controller
kubectl delete -f traefik-ingres.yaml
#At the moment it is not working