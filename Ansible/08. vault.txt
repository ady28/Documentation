#Create a new file in vault
ansible-vault create secret-variables.yml
#It will open vim
type in
pass: "secret"
ansible-vault edit secret-variables.yml

#Use the file in check-status.yml
#Before tasks put
vars_files:
  - ../secret-variables.yml
#Place a debug task in the file
      - name: Debug vault contents
        debug:
          msg: "{{ pass }}"
#Run the playbook with --ask-vault-pass or --vault-password-file after putting the password in a text file

#When testing playbooks you an put also ansible_become_pass as a variable in the encrypted file and avoid using --ask-become-pass